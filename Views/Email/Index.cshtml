@model IEnumerable<EmployeeVotingSystem.Models.Email>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EmployeeID)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EmployeeID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Email_Address">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Email_Address">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Email_Address">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
