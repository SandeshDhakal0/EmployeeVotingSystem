@model IEnumerable<EmployeeVotingSystem.Models.Address>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                Address Id
            </th>
            <th>
                Street No
            </th>
            <th>
                Postal Code
            </th>
            <th>
                Address Type
            </th>
            <th>
                City
            </th>
            <th>
                Country
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.address_id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.street_no)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.postal_code)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.address_type)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.city)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.country)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.address_id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.address_id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.address_id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
